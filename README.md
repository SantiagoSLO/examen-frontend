# Restaurante Frontend

Static frontend for the restaurant application.

## Features

- Display menu categories and products
- Fetch data from backend API
- Responsive design with Bootstrap

## Deployment

Deployed on GitHub Pages.

## Setup

Open `index.html` in a browser. Update the `backendUrl` variable with the deployed backend URL.

## Connecting to Backend

The frontend fetches data from the backend API endpoints.